plugins {
  id 'maven-publish'
}

apply from: "${rootDir}/gradle/java.gradle"

dependencies {
  compile deps.slf4j
}

publishing {
  publications {
    mavenJava(MavenPublication) {
      from components.java
    }
  }
}
